Pi
==

Command-line tool for managing development environments. It is configurable
with ``pi.yaml`` file, which is like ``Makefile`` on steroids - with nice
commands UI, containerized environments and services, which are required in
order to run your commands.

.. toctree::
    :maxdepth: 2

    types
